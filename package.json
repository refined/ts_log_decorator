{
  "name": "method-log-decorator",
  "version": "1.0.3",
  "description": "A lightweight TypeScript library providing easy-to-use logging decorators.",
  "main": "out/index.js",
  "types": "out/index.d.ts",
  "scripts": {
    "start": "tsc -p tsconfig.json && node --max-old-space-size=16000 src/index.js && exit 1",
    "test": "jest",
    "build": "tsc",
    "reinstall": "npm install && npm run build && npm install --package-lock-only"
  },
  "keywords": ["typescript", "decorator", "logging", "logger", "log"],
  "author": "Fedor Novikov",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  }
}
